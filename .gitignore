# Environment and Secrets files
.env
.env.local
.env.*.local

# Dependencies (NEVER commit this!)
node_modules/

# Ignore local test images and generated JSON so local runs don't get committed
assets/images/*
!assets/images/.gitkeep
assets/gallery.json